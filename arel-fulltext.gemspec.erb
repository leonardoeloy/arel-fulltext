# # -*- encoding: utf-8 -*-
# This is a generated file. Use rake to update the gemspec file list.
$:.push File.expand_path("../lib", __FILE__)
require "arel"

Gem::Specification.new do |s|
    s.name        = "arel-fulltext"
    s.version     = '1.0.0'
    s.platform    = Gem::Platform::RUBY
    s.authors     = ["Leonardo Eloy"]
    s.email       = ["leonardo.eloy@gmail.com"]
    s.homepage    = "https://github.com/leonardoeloy/arel-fulltext"
    s.description = "Adds 'native' full-text search support to Arel."
    s.summary     = "Adds 'native' full-text search support to Arel."
    s.license     = %q{MIT}

    s.rdoc_options = ["--main", "README.md"]
    s.extra_rdoc_files = ["LICENSE", "README.md"]

    s.files         = [<%= files.map(&:inspect).join ',' %>]
    s.require_paths = ["lib"]

    s.add_development_dependency('minitest', '~> 5.4')
    s.add_development_dependency('rdoc', '~> 4.0')
    s.add_development_dependency('arel', '~> 6.0.0')
    s.add_development_dependency('rake')
end